<%= semantic_form_for @mail_template, url: [:process_finished, :admin, @recording] do |f| %>
  <%= render partial: "inputs", locals: { f: f } %>
  <%= f.actions do %>
    <%= f.action :submit, label: "Finish Recording" %>
  <% end %>
<% end %>
