<%= semantic_form_for @mail_template, url: [:process_confirmed, :admin, @recording] do |f| %>
  <%= render partial: "inputs", locals: { f: f } %> 
  <%= f.actions do %>
    <%= f.action :submit, label: "Confirm Recording" %>
  <% end %>
<% end %>
